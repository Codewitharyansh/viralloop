{
  "name": "viraloop-backend",
  "version": "2.0.0",
  "description": "Viraloop — turn any website into a viral Instagram carousel, learn from every post. Image generation is done locally (free) via Playwright-rendered HTML slides instead of a paid AI image API.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "playwright": "^1.47.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
